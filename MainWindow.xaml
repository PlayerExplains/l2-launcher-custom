<Window x:Class="L2Launcher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="L2 Launcher" Width="1280" Height="720" Icon="/Assets/launcher.ico" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.Resources>

        <Style x:Key="SocialIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#00000000"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Opacity" Value="0.6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.88"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.35"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ChromeButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#EAEAEA"/>
            <Setter Property="Background" Value="#151515"/>
            <Setter Property="BorderBrush" Value="#2E2E2E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="IconOverlayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#00000000"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StartButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#F3E3CF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#00000000"/>
            <Setter Property="BorderBrush" Value="#00000000"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Width="150" Height="150">
                            <Ellipse Fill="#01000000"/>

                            <Ellipse x:Name="HoverGlow" Margin="10" Fill="#00FFFFFF" Stroke="#00F6C287" StrokeThickness="2"/>

                            <TextBlock Text="START" Foreground="{TemplateBinding Foreground}" FontWeight="{TemplateBinding FontWeight}" FontSize="{TemplateBinding FontSize}" FontFamily="{TemplateBinding FontFamily}" HorizontalAlignment="Center" VerticalAlignment="Center" TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
                                <TextBlock.Effect>
                                    <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="#66000000" Opacity="0.8"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HoverGlow" Property="Fill" Value="#18F3A24E"/>
                                <Setter TargetName="HoverGlow" Property="Stroke" Value="#88FFD09A"/>
                                <Setter Property="Foreground" Value="#FFF1E1"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="HoverGlow" Property="Fill" Value="#22D97B32"/>
                                <Setter TargetName="HoverGlow" Property="Stroke" Value="#AAFFD8A8"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#8F8478"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                                <Setter TargetName="HoverGlow" Property="Fill" Value="#00000000"/>
                                <Setter TargetName="HoverGlow" Property="Stroke" Value="#00000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FullCheckButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#CFC2B2"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#00000000"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Width="150" Height="34">
                            <Border x:Name="FullCheckGlow" Background="#00000000" CornerRadius="8"/>

                            <TextBlock Text="Full Check" Foreground="{TemplateBinding Foreground}" FontSize="{TemplateBinding FontSize}" FontFamily="{TemplateBinding FontFamily}" FontWeight="{TemplateBinding FontWeight}" HorizontalAlignment="Center" VerticalAlignment="Center" TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
                                <TextBlock.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Color="#77000000" Opacity="0.9"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="FullCheckGlow" Property="Background" Value="#14F0A04B"/>
                                <Setter Property="Foreground" Value="#FFF0DE"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="FullCheckGlow" Property="Background" Value="#20D37A33"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#7B726A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" Background="#040404" BorderBrush="#161616" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#0B0B0B" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="L2 Launcher" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="#C8C8C8" FontSize="12"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,6,0">
                    <Button Width="34" Height="22" Margin="0,3,4,3" Style="{StaticResource ChromeButtonStyle}" Click="MinimizeButton_Click">—</Button>
                    <Button Width="34" Height="22" Margin="0,3,0,3" Style="{StaticResource ChromeButtonStyle}" Background="#351717" BorderBrush="#5C2A2A" Click="CloseButton_Click">X</Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Background="Black">
                <Viewbox Stretch="Uniform">
                    <Canvas Width="1376" Height="768">
                        <Image Width="1376" Height="768" Canvas.Left="0" Canvas.Top="0" Source="/Assets/background.png" Stretch="Fill"/>

                        <Button x:Name="GameStartButton" Canvas.Left="617" Canvas.Top="517" Width="150" Height="150" Style="{StaticResource StartButtonStyle}" Click="GameStartButton_Click" IsEnabled="False"/>

                        <Button x:Name="FullCheckButton" Canvas.Left="614" Canvas.Top="664" Width="150" Height="30" Style="{StaticResource FullCheckButtonStyle}" Click="FullCheckButton_Click"/>

                        <Button Canvas.Left="1030" Canvas.Top="590" Width="34" Height="34" Style="{StaticResource SocialIconButtonStyle}" Click="HomeButton_Click" ToolTip="Home">
                            <Border BorderBrush="#A47A4E" BorderThickness="1" CornerRadius="4" Padding="2" Background="#12000000">
                                <Image Source="/Assets/icon-home.png" Width="22" Height="22" Stretch="Uniform"/>
                            </Border>
                        </Button>

                        <Button Canvas.Left="1065" Canvas.Top="590" Width="34" Height="34" Style="{StaticResource SocialIconButtonStyle}" Click="DiscordButton_Click" ToolTip="Discord">
                            <Border BorderBrush="#A47A4E" BorderThickness="1" CornerRadius="4" Padding="2" Background="#12000000">
                                <Image Source="/Assets/icon-discord.png" Width="22" Height="22" Stretch="Uniform"/>
                            </Border>
                        </Button>

                        <Button Canvas.Left="1100" Canvas.Top="590" Width="34" Height="34" Style="{StaticResource SocialIconButtonStyle}" Click="FacebookButton_Click" ToolTip="Facebook">
                            <Border BorderBrush="#A47A4E" BorderThickness="1" CornerRadius="4" Padding="2" Background="#12000000">
                                <Image Source="/Assets/icon-facebook.png" Width="22" Height="22" Stretch="Uniform"/>
                            </Border>
                        </Button>

                        <Border Canvas.Left="285" Canvas.Top="700" Width="815" Height="15" Background="#00000000" CornerRadius="6" ClipToBounds="True">
                            <Rectangle x:Name="ProgressFill" Width="0" Height="19" HorizontalAlignment="Left" Fill="#7A4A24"/>
                        </Border>

                        <TextBlock x:Name="ProgressText" Canvas.Left="430" Canvas.Top="735" Width="520" Height="26" Text="" Foreground="#D8D0C8" FontSize="15" FontFamily="Georgia" TextAlignment="Center" FontWeight="SemiBold" TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType"/>
                    </Canvas>
                </Viewbox>
            </Grid>
        </Grid>
    </Border>
</Window>